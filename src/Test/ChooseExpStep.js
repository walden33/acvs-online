/**
 * 
 */
 = class extends util.AbstractStep {

    // A placeholder empty constructor
    constructor() {
        super();
    }

    execute() {
        const ws = util.Workspace.workspace();
        ws.append("div")
    }
}